Uni Sport Hub — source images

Your original PNG files live in this folder (e.g. ChatGPT Image … .png).

For the React app, the same photos are copied into `client/src/images/` with short, stable names so imports work with Create React App:

  jersey-uniform.png
  training-jersey-tee.png
  silicone-bangles.png
  player-id-card.png
  stationery-set.png
  baseball-cap.png
  water-bottle.png
  sports-duffel-bag.png

If you replace an image here, copy the new file over the matching name in `client/src/images/` (or re-run your sync) so the marketplace shows the update.

Product data: `client/src/data/marketplaceStoreProducts.js`
